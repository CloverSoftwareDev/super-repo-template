# @super-repo/api-client

Generated TypeScript client, React Query hooks, and Faker mocks for the API.

## Generation

Ensure the API is running at `http://localhost:3000` (or update `kubb.config.ts`).

```bash
npm run generate
```

## Usage

```typescript
import { useGetHealth } from '@super-repo/api-client/hooks'
```
